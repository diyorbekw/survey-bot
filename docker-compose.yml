version: '3.8'

services:
  interview-bot:
    build: .
    container_name: telegram-interview-bot
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot-data: